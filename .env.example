# Copy this file to .env for local development.
# For production (Vercel), set this same variable in the project's
# Environment Variables settings instead of committing a .env file.

REACT_APP_BACKEND_URL=http://localhost:3000
